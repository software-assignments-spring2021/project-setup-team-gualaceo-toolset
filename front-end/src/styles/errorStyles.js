const styles = (theme) => ({
  root: {
    position: "absolute",
    zIndex: 5,
    marginTop: "-10px",
    left: theme.spacing(2),
    right: theme.spacing(2),
    padding: theme.spacing(2),
  },
  close: { marginLeft: "-47px" },
});

export default styles;
